import React from 'react';

const Button = (props) => {
  console.log(props);
  return (
    <div className="flex justify-center ">
      <button>5</button>
    </div>
  );
};

export default Button;
